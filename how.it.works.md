# How It Works — Pure Responsive DIV Generator

> Comprehensive technical reference covering every function, data structure, scenario, and edge case in the entire project.

---

## Table of Contents

1. [Project Architecture](#1-project-architecture)
2. [File Map](#2-file-map)
3. [Data Model](#3-data-model)
4. [Utility Functions](#4-utility-functions)
5. [State Management](#5-state-management)
6. [Tree Operations](#6-tree-operations)
7. [Responsive Property System](#7-responsive-property-system)
8. [Canvas Rendering Pipeline](#8-canvas-rendering-pipeline)
9. [Inspector Panel (renderProps)](#9-inspector-panel-renderprops)
10. [DOM Tree View](#10-dom-tree-view)
11. [Chrome & Status Bar](#11-chrome--status-bar)
12. [Export System](#12-export-system)
13. [Event System & Interactions](#13-event-system--interactions)
14. [CSS Architecture](#14-css-architecture)
15. [MCP Server](#15-mcp-server)
16. [Edge Cases & Guards](#16-edge-cases--guards)
17. [Property Reference Table](#17-property-reference-table)
18. [Verification](#18-verification)

---

## 1. Project Architecture

```
┌──────────────────────────────────────────────────────┐
│                    index.html                         │
│  ┌──────────┐  ┌───────────────┐  ┌──────────────┐  │
│  │ Left     │  │ Canvas        │  │ Right Panel  │  │
│  │ Panel    │  │ (Wireframe    │  │ (Inspector   │  │
│  │ (Presets │  │  Preview)     │  │  Properties) │  │
│  │  & Tree) │  │               │  │              │  │
│  └──────────┘  └───────────────┘  └──────────────┘  │
│  ┌──────────────────────────────────────────────────┐│
│  │             Status Bar                            ││
│  └──────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────┘
         │                    │
    builder.js           builder.css
    (1563 lines)         (1122 lines)
         │
         ├──────────────► generator.js ◄──────────────┐
         │                (411 lines)                    │
         │          THE SINGLE SOURCE OF TRUTH        │
         │        for all HTML & CSS emission         │
    export-template.css                               │
                                                      │
    mcp-server/                                       │
    ├── index.js  (MCP tools, 344 lines)                  │
    └── core.js   (server-side state, 373 lines) ───────────┘
```

### Why `generator.js` exists
`builder.js` and `mcp-server/core.js` used to carry **two copies** of the emitter.
They drifted: the MCP copy silently dropped `overflow-x`/`overflow-y` and the
tablet/mobile `horizontalAlign` blocks, so an agent calling `set_props` got a
success response and CSS that ignored the properties it had set.

`generator.js` is pure and state-free — every entry point takes the tree and
breakpoints as arguments. Both surfaces import it and keep only thin
state-bound wrappers. **A change to emitted CSS belongs in `generator.js` and
reaches the browser and the MCP server at once.**

### Design Principle
**Only `<div>` elements.** The user's rule: _"nothing just responsive div's need #do not upgrade by inject other elements."_ Every node in the tree renders as a `<div>`. No `<section>`, `<header>`, `<span>`, etc.

### Runtime
- Pure vanilla JavaScript (ES5-style, no transpiler)
- `builder.js` is an ES module whose body is wrapped in an IIFE `(function() { "use strict"; ... })()`
- No framework, no build step, no npm for the browser app
- Served via `python3 -m http.server 8080`

---

## 2. File Map

| File | Lines | Purpose |
|---|---|---|
| `index.html` | 244 | Page structure: toolbar, left panel (presets + tree tabs), canvas frame, right panel (inspector), export modal, status bar |
| `builder.js` | 1563 | Browser app: state, tree ops, rendering, events, UI. An **ES module** — `index.html` loads it with `<script type="module">`, so the builder must be served over HTTP (`file://` will not work) |
| `generator.js` | 411 | **Single source of truth for HTML & CSS emission.** Pure, state-free, shared by the browser and the MCP server |
| `builder.css` | 1122 | Dark theme UI styling, layout grid, component styles |
| `export-template.css` | ~20 | CSS reset template included in exported documents |
| `mcp-server/index.js` | 344 | MCP server with 21 tools |
| `mcp-server/core.js` | 373 | Server-side **state** (tree, history, breakpoints). Delegates all emission to `generator.js` |
| `generate_examples.js` | ~1100 | Generates all 10 examples in `examples/` through `core.js` — no handwritten HTML |

---

## 3. Data Model

### 3.1 Node Structure

Every div in the tree is a **node object**:

```javascript
{
  id: "div_a4xk2",            // Unique ID (generated by uid())
  type: "div",                 // Always "div"
  name: "Header",             // Display name (shown in tree)
  customClass: "header",       // CSS class for export
  responsive: {
    desktop: { ... },          // Base properties (always present)
    tablet: { ... },           // Override properties (sparse)
    mobile: { ... }            // Override properties (sparse)
  },
  children: [ ... ]            // Array of child node objects
}
```

**Key insight:** `desktop` contains ALL properties (merged with defaults). `tablet` and `mobile` only contain **overridden** properties. This is a **sparse override** pattern.

### 3.2 The Root Node

```javascript
State.root = {
  id: "root",                  // Special ID, never rendered as a div
  type: "div",
  name: "Page Root",
  customClass: "page-layout",
  responsive: {
    desktop: {
      display: "flex",
      flexDirection: "column",
      gap: 20,
      paddingTop: 20, paddingRight: 20,
      paddingBottom: 20, paddingLeft: 20,
      minHeight: 600,
      justifyContent: "flex-start",
      alignItems: "stretch",
      horizontalAlign: "stretch",
      maxWidth: ""
    },
    tablet: {},
    mobile: {}
  },
  children: []                 // Top-level divs go here
}
```

The root is **never exported as a `<div>`** — it's a virtual container. Its children are the actual top-level elements.

### 3.3 defaultDesktopProps (58 Properties)

Every new node gets these defaults merged into `responsive.desktop`:

| Category | Properties |
|---|---|
| **Layout** | `display`, `columns`, `customColumns`, `gridAutoMode`, `gridMinColWidth`, `justifyItems`, `alignContent`, `flexDirection`, `flexWrap`, `justifyContent`, `alignItems` |
| **Alignment** | `horizontalAlign`, `textAlign` |
| **Sizing** | `width`, `maxWidth`, `height`, `maxHeight`, `minHeight`, `aspectRatio` |
| **Spacing** | `paddingTop/Right/Bottom/Left`, `paddingLinked`, `marginTop/Right/Bottom/Left`, `marginLinked`, `gap`, `rowGap` |
| **Position** | `position`, `top`, `right`, `bottom`, `left`, `zIndex` |
| **Overflow** | `overflow`, `overflowX`, `overflowY` |
| **Visual** | `backgroundColor`, `borderWidth`, `borderStyle`, `borderColor`, `borderRadius`, `boxShadow`, `opacity`, `transform`, `transition`, `backdropFilter` |
| **Child** | `span`, `flexGrow`, `flexShrink`, `flexBasis`, `order` |
| **Misc** | `hidden`, `direction` |

---

## 4. Utility Functions

### `uid(prefix)` — Line 8
Generates unique IDs like `"div_a4xk2"`.
- Uses `Math.random().toString(36)` for entropy
- Appends an incrementing counter to guarantee uniqueness
- Called every time a new node is created

### `debounce(fn, delay)` — Line 14
Delays function execution until user stops typing.
- Used on all text inputs in the inspector (300ms delay)
- Prevents re-rendering on every keystroke
- **Scenario:** User types "hero-container" → only commits once after 300ms of no typing

### `clone(val)` — Line 23
Deep clones via `JSON.parse(JSON.stringify())`.
- Used by History snapshots and duplicateNode
- **Limitation:** Cannot clone functions, Dates, undefined values (not needed here)

### `el(tag, className, text)` — Line 27
DOM element factory. Creates `<tag class="className">text</tag>`.
- Used everywhere in rendering (canvas, tree, inspector)
- Returns a real DOM element
- **Scenario:** `el("button", "btn-quick-action", "Split 2")` → `<button class="btn-quick-action">Split 2</button>`

---

## 5. State Management

### 5.1 The State Object — Line 108

```javascript
var State = {
  device: "desktop",           // Current editing breakpoint
  selectedId: null,            // Currently selected node ID
  activeTab: "palette",        // Left panel tab: "palette" | "tree"
  exportTab: "combined",       // Export modal tab: "combined" | "html" | "css"
  breakpoints: {               // Configurable breakpoint values
    tablet: 992,
    mobile: 576
  },
  root: { ... }                // The node tree (see section 3.2)
};
```

### 5.2 History System — Line 140

```javascript
var History = {
  past: [],       // Stack of snapshots (max 50)
  future: [],     // Redo stack
  limit: 50
};
```

**Functions:**
| Function | What it does |
|---|---|
| `History.snapshot()` | Deep clones `State.root` + `State.selectedId` |
| `History.push()` | Saves snapshot to `past[]`, clears `future[]` |
| `History.undo()` | Moves current state to `future[]`, restores from `past[]` |
| `History.redo()` | Moves current state to `past[]`, restores from `future[]` |

### 5.3 The `commit()` Pattern — Line 168

```javascript
function commit(fn) {
  History.push();
  fn();
  render();
}
```

**This is the most important pattern in the codebase.** Every mutation goes through `commit()`:
1. Snapshots the current state (for undo)
2. Executes the mutation function
3. Re-renders the entire UI

**Scenario:** User clicks "Split 2 Cols" → `commit(function() { splitNode(id, 2); })` → snapshot saved → tree modified → canvas + tree + inspector + status bar all re-render.

---

## 6. Tree Operations

### `createPureDiv(name, customClass, desktopProps, tabletProps, mobileProps)` — Line 224
Creates a new node with merged defaults.
```javascript
var node = createPureDiv("Header", "header", { display: "flex" });
// → node.responsive.desktop = { ...defaultDesktopProps, display: "flex" }
// → node.responsive.tablet = {}
// → node.responsive.mobile = {}
```

### `createPresetDiv(presetKey)` — Line 239
Factory for preset layout blocks. Available presets:

| Key | What it creates |
|---|---|
| `"container"` | Flex column, max-width 1200px, centered |
| `"box"` | Simple block div |
| `"grid2"` | 2-column grid with 2 child divs; tablet: 2 cols, mobile: 1 col |
| `"grid3"` | 3-column grid with 3 child divs; tablet: 2 cols, mobile: 1 col |
| `"grid4"` | 4-column grid with 4 child divs; tablet: 2 cols, mobile: 1 col |
| `"grid12"` | 12-column grid with span-4 + span-8 children |
| `"flex-row"` | Flex row with wrap; 2 items (grow 1 + grow 2) |
| `"flex-col"` | Flex column stack; 2 items |

**Scenario:** User clicks the "3-Col Grid" preset card → `createPresetDiv("grid3")` → creates parent grid + 3 child divs → all inserted into tree.

### `walk(node, visit, parent, index)` — Line 344
Recursive depth-first tree traversal.
- Visits every node in the tree
- `visit` callback receives `(node, parent, index)`
- Return `false` from `visit` to stop early
- Used by: `findNode`, `findParent`, `countNodes`, `duplicateNode`, CSS generator

### `findNode(id)` — Line 352
Finds a node by ID anywhere in the tree.
- Returns the node object or `null`
- Uses `walk()` with early termination

### `findParent(id)` — Line 364
Finds the parent of a node by the child's ID.
- Returns the parent node or `null`
- **Scenario:** `findParent("div_x")` → walks tree until it finds a node whose `children[]` contains `"div_x"`, returns that parent

### `insertNode(parentId, node, index)` — Line 375
Inserts a node into a parent's children at a specific index.
- If `index` is omitted, appends to the end
- Used when inserting preset divs

### `removeNode(id)` — Line 383
Removes a node from its parent's children array.
- **Guard:** Cannot delete root (`id === "root"` check)
- **Side effect:** Clears `State.selectedId` if the deleted node was selected
- **Cascade:** All children are removed with the parent (garbage collected)

### `moveNode(id, delta)` — Line 393
Moves a node up (`delta = -1`) or down (`delta = +1`) within its siblings.
- **Guard:** Boundary check prevents moving before index 0 or past last index
- **Scenario:** 3 children `[A, B, C]` → moveNode(B, -1) → `[B, A, C]`

### `duplicateNode(id)` — Line 405
Deep-clones a node and inserts the copy after the original.
- **Critical:** Walks the clone to reassign ALL IDs (prevents ID collisions)
- **Side effect:** Selects the new copy
- **Scenario:** Clone a 3-col grid → creates an independent copy with new IDs for parent + all children

### `splitNode(id, colCount)` — Line 418
Converts a node into a grid container with N equal columns.
- Sets `display: grid`, `columns: N`
- **Auto-responsive:** Tablet gets `min(N, 2)` columns, mobile gets `1`
- **BUG 1 FIX:** Only adds child divs if node has no children (prevents double-split from creating duplicates)
- **Scenario:** Empty div → splitNode(id, 3) → becomes grid with 3 child divs. Already has children → just updates grid settings.

### `addChildDiv(id)` — Line 439
Adds a single child div to any node.
- Creates a "nested-div" class child
- Selects the new child after creation

### `wrapInParent(id)` — Line 449
Wraps an existing node inside a new parent div.
- The wrapper becomes a flex-column with gap and padding
- Original node becomes the wrapper's child
- **Scenario:** `[root → A]` → wrapInParent(A) → `[root → Wrapper → A]`

### `countNodes()` — Line 467
Counts all non-root nodes in the tree.
- Used by status bar to show "12 divs"

---

## 7. Responsive Property System

### 7.1 `getEffectiveProps(node, device)` — Line 193

This is the **core responsive algorithm**. It computes the final CSS properties for any node at any breakpoint.

**Algorithm:**
```
1. Start with defaultDesktopProps (all 55 defaults)
2. Merge node.responsive.desktop over defaults
3. If device is "tablet":
   - Merge node.responsive.tablet over result
4. If device is "mobile":
   - Merge node.responsive.tablet over result (cascade!)
   - Then merge node.responsive.mobile over result
5. Run migration for old padding format
6. Return final merged object
```

**Key: The Cascade.** Mobile inherits from tablet, which inherits from desktop. If you set `columns: 2` on tablet and don't set anything on mobile, mobile ALSO gets `columns: 2`.

**Migration (Lines 212-218):** Old nodes stored `padding` as a single number. The migration detects this and expands it to `paddingTop/Right/Bottom/Left`.

### 7.2 How Properties Flow

```
User sets "columns: 3" on desktop
User sets "columns: 1" on mobile
User sets nothing on tablet

getEffectiveProps(node, "desktop")  → columns: 3
getEffectiveProps(node, "tablet")   → columns: 3  (inherited from desktop)
getEffectiveProps(node, "mobile")   → columns: 1  (overridden)
```

### 7.3 The `setProp()` Helper (Inside renderProps)

```javascript
function setProp(key, val) {
  commit(function () {
    if (!node.responsive[dev]) node.responsive[dev] = {};
    node.responsive[dev][key] = val;
  });
}
```

Every inspector control calls `setProp()`. This:
1. Creates the device override object if it doesn't exist
2. Sets the property
3. Commits (snapshot + re-render)

---

## 8. Canvas Rendering Pipeline

### 8.1 `renderCanvas()` — Line 736

1. Clears `canvas.innerHTML`
2. If root has no children → shows empty state message
3. Otherwise, iterates root's children and calls `renderCanvasNode()` for each

### 8.2 `renderCanvasNode(node, parentNode, depth)` — Line 500

This is the **largest function** (~230 lines). It creates the visual wireframe for a single div.

**Step-by-step:**

1. **Compute effective props** for the current device
2. **Determine parent's display** mode (needed for flex child rules)
3. **Create wrapper div** (`<div class="struct-div">`)
4. **Apply depth-based coloring** (purple → teal → amber → blue cycle)
5. **Apply display mode:**
   - `grid` → sets `display:grid`, `grid-template-columns`, gap, row-gap, justify-items, align-content
   - `flex` → sets `display:flex`, direction, wrap, justify, align, gap
   - `block` → sets `display:block`
6. **Apply alignment:**
   - `center` → `margin-left:auto; margin-right:auto; justify-self:center`
   - `right` → `margin-left:auto; margin-right:0; justify-self:end`
   - `left` → `margin-left:0; margin-right:auto; justify-self:start`
   - `stretch` → `width:100%` (unless grid child)
7. **Apply width** and text-align
8. **Apply 4-side padding** (T/R/B/L → CSS `padding` shorthand)
9. **Apply 4-side margin** (only if explicitly set)
10. **Apply sizing:** minHeight, height, maxHeight, maxWidth, aspectRatio
11. **Apply grid/flex child rules:** span, flexGrow, flexShrink, flexBasis, order
12. **Apply position:** static/relative/absolute/fixed/sticky + offsets + z-index
13. **Apply overflow:** visible/hidden/scroll/auto + overflowX/Y
14. **Apply visual styles:** backgroundColor, border, borderRadius, boxShadow, opacity
15. **Apply direction** (RTL)
16. **Add tag badge** (`<div.header>`)
17. **Add floating toolbar** (if selected) with alignment, move, add, split, clone, delete buttons
18. **Render children** recursively, or show empty slot indicator

### 8.3 Depth Coloring

```javascript
var depthColors = [
  "var(--indigo-soft)", // Depth 0: purple
  "#10b98126",          // Depth 1: teal
  "#f59e0b1a",          // Depth 2: amber
  "#38bdf826"           // Depth 3: blue
];
wrapper.style.borderColor = depthBorders[depth % 4];
```

This creates a visual hierarchy — parent divs are purple, children teal, grandchildren amber, etc.

### 8.4 The Floating Toolbar

When a node is selected, it gets a toolbar with these buttons:

| Button | Action | Shortcut |
|---|---|---|
| ⭰ Left | Set `horizontalAlign: "left"` | — |
| ⭿ Center | Set `horizontalAlign: "center"` | — |
| ⭲ Right | Set `horizontalAlign: "right"` | — |
| ⭄ Stretch | Set `horizontalAlign: "stretch"` | — |
| ↑ | Move up in siblings | — |
| ↓ | Move down in siblings | — |
| + Child | Add child div | — |
| Split 2 | Split into 2-column grid | — |
| Clone | Duplicate node + children | — |
| ✕ | Delete node | Delete/Backspace key |

---

## 9. Inspector Panel (renderProps)

### 9.1 Overview — Line 792

`renderProps()` builds the entire right panel dynamically. It creates **8 cards**:

### Card 1: DIV Identity
- CSS class name input (debounced 300ms)
- Device badge showing current editing breakpoint
- "Reset to Desktop" button (for tablet/mobile modes)
- **Hidden on this breakpoint:** checkbox toggle (`hidden: true` emits `display: none` in CSS export)

### Card 2: Alignment & Width
- Horizontal alignment segmented bar: Left | Center | Right | Stretch
- Width text input with placeholder "auto / 50% / 300px"
- Quick width presets: Auto, 25%, 33%, 50%, 75%, 100%
- Direction toggle: Inherit | LTR → | ← RTL
- **Text Align segmented bar:** Left | Center | Right | Justify

### Card 3: Container Display
- Display mode: GRID | FLEX | BLOCK

**When GRID is selected:**
- Grid Mode: Fixed Cols | Auto-Fit | Auto-Fill
- If Auto-Fit/Fill: Min Column Width input
- If Fixed: Grid Columns number input (1-12)
- **Custom Template input:** text input for custom `grid-template-columns` (e.g. `1fr 3fr / 200px 1fr 1fr`), overrides fixed columns
- Col Gap + Row Gap (px)
- Justify Items dropdown: Stretch | Start | Center | End
- Align Content dropdown: Start | Center | End | Stretch | Between | Around

**When FLEX is selected:**
- Flex Direction: Row → | Column ↓
- Flex Wrap: No Wrap | Wrap
- Justify dropdown: Start | Center | End | Between | Around
- Align Items dropdown: Stretch | Start | Center | End
- Flex Gap (px)

### Card 4: Sizing & Spacing
- Min Height (px) + Height (text: "auto / 100vh")
- Max Width + Max Height (text)
- Aspect Ratio presets: Auto | 1:1 | 4:3 | 16:9 | 21:9
- **4-side Padding:** 4 number inputs (T/R/B/L) + 🔗 Linked toggle
- **4-side Margin:** 4 text inputs (T/R/B/L) + 🔗 Linked toggle

**Linked mode:** Changing any one side updates all 4 sides to the same value.

### Card 5: Position & Layer
- Position: Static | Rel | Abs | Fixed | Sticky
- If non-static: Top/Right/Bottom/Left offset inputs + Z-Index
- Combined Overflow: Visible | Hidden | Scroll | Auto
- **Per-Axis Overflow:** Overflow-X and Overflow-Y dropdown selectors (`inherit`, `visible`, `hidden`, `scroll`, `auto`)

### Card 6: Visual Style
- Background Color: color picker + hex text input + Clear button
- Border: Width input + Style dropdown (None/Solid/Dashed/Dotted) + Color picker
- Border Radius: presets (0/4px/8px/12px/50%) + custom text input
- Box Shadow: presets (None/sm/md/lg)
- Opacity: range slider (0-1, step 0.05)

### Card 7: Grid/Flex Child Rules
- Span (1-12), Grow (0+), Order (integer)
- Shrink (0+), Basis (text: "auto / 0 / 200px")

### Card 8: Quick Layout Splits
- Split 2 Cols, Split 3 Cols, + Child Div, Wrap in Div, Delete Div

---

## 10. DOM Tree View

### `renderTree()` — Line 756

Builds a flat indented list representing the node hierarchy.

**Each tree node shows:**
- Indentation based on depth (`depth * 14 + 8` px)
- Tag name: `div`
- Class name: `.header` or `#a4xk` (ID fallback)
- Type badge: `grid (3c)`, `flex-row`, `flex-col`, `block`
- Alignment indicator: `· center`, `· right` (if not stretch)

**Selection:** Clicking a tree node sets `State.selectedId` and re-renders.

---

## 11. Chrome & Status Bar

### `renderChrome()` — Line 1232

Updates the toolbar and status bar:
- Undo/Redo button disabled states
- Node count: "12 divs"
- Selection info: "Selected: `<div.header>`"
- Active breakpoint indicator: "Desktop (Base > 992px)"
- **Live Breakpoints Editor:** Inline inputs in status bar (`Tablet [ 992 ]px`, `Mobile [ 576 ]px`) that immediately update responsive preview & CSS export
- Frame `data-device` attribute (controls canvas width in CSS)

### `render()` — Line 1258

The master render function. Calls all 4 renderers:
```javascript
function render() {
  renderCanvas();    // Wireframe preview
  renderTree();      // DOM tree sidebar
  renderProps();     // Inspector panel
  renderChrome();    // Toolbar + status bar
}
```

---

## 12. Export System

### 12.1 `generateCleanHtml(node, depth)` — Line 1268

Recursively generates pure HTML with proper indentation.

**Rules:**
- Root node → skip, just render children
- Each non-root node → `<div class="className">` + children + `</div>`
- If `direction: "rtl"` on desktop → adds `dir="rtl"` attribute
- Falls back to `class="div-box"` if no custom class

**Output example:**
```html
<div class="container">
  <div class="grid-3col">
    <div class="col"></div>
    <div class="col"></div>
    <div class="col"></div>
  </div>
</div>
```

### 12.2 `generateResponsiveCss(root, breakpoints)` — `generator.js`

**Table-driven.** One function, `declarationsFor(props, ctx)`, produces the
complete ordered declaration list for a device. Desktop writes it directly;
each breakpoint is produced by *diffing* that list against the wider device.

```
declarationsFor(effective(node,"desktop"), ctx("desktop"))  ─► base rule
declarationsFor(effective(node,"tablet"),  ctx("tablet"))   ─┐
                                                             ├─► diff ─► @media 992
                                                            ─┘
declarationsFor(effective(node,"mobile"),  ctx("mobile"))   ─┐
                                                             ├─► diff ─► @media 576
                                          (vs the TABLET list)┘
```

Mobile diffs against **tablet**, not desktop: both media blocks are
`max-width`, so at 400px the tablet rule applies and the mobile rule overrides
it. Diffing against desktop would restate what tablet already said.

#### Why this shape
The breakpoint blocks used to be a hand-maintained chain of `if` statements
running parallel to the desktop chain. Anything nobody remembered to add was
dropped **without an error**, while `set_props` still reported success. An
audit that set each property on each device and checked whether it reached the
CSS measured 43/43 on desktop but only **27/43** at breakpoints — `top`,
`right`, `bottom`, `left`, `zIndex`, `maxWidth`, `justifyContent`,
`alignItems`, `flexWrap` and others were unreachable. Desktop and both
breakpoints now run the same code, so a property added once is automatically
overridable everywhere. The audit measures **47/47** at all three devices.

#### Neutral values
When a declaration is present at the wider device and absent at the narrower
one, a neutral is emitted so the cascade cannot leak the wider rule down. This
is what makes *clearing* a property at a breakpoint possible at all:

```css
@media (max-width: 576px) { .card { border: none; max-width: none; } }
```

Neutrals mirror this generator's own defaults (`align-items: stretch`), not the
CSS-wide initial values. `row-gap` deliberately has **no** neutral: `gap` is
always emitted alongside it and already sets the row gap, so `row-gap: normal`
would cancel it.

#### Parent context (`ctx`)
Resolved per device, because a parent can switch between grid, flex-row and
flex-column at a breakpoint:

| ctx | Effect |
|---|---|
| `gridParent` | `justify-self` is emitted **only** here. Chrome honours `justify-self` in block layout, where it forces shrink-to-fit and bursts the container — emitting it outside a grid collapsed every centered container |
| `rowParent` \|\| `gridParent` | `min-width: 0`. Flex items **and** grid items both have an automatic minimum size of min-content; without this a nowrap row bursts its container and a grid track is widened by its widest item until the grid overflows |

#### `hidden` is the display value
Not a flag appended after the diff. Being part of the declaration list is what
lets a breakpoint turn a div back **on**: a div hidden on desktop emits
`display: none` in the base rule and `display: flex` in its mobile block — the
sidebar/hamburger pair.

#### Auto-fit tracks
`repeat(auto-fit, minmax(min(240px, 100%), 1fr))`. The inner `min()` is
required: a raw `minmax(240px, 1fr)` track cannot shrink below 240px and
overflows narrow viewports.

### 12.3 `generateFullHtmlDocument(root, breakpoints)`

Assembles `<!DOCTYPE html>` + `<meta viewport>` + inline `<style>` + the pure
div body into one standalone, dependency-free file.

### 12.4 Export warnings

Before showing the code, the modal scans the tree for divs that carry mobile
rules but no tablet rules and names the uncovered pixel range:

> ⚠ 3 divs have mobile rules but no tablet rules (.card, .hero, .grid) — unstyled between 577px and 992px.

This exists because the three fixed preview widths (1280 / 768 / 375) can all
look correct while a whole band between them has no rules at all.

---

## 13. Event System & Interactions

### 13.1 Preset Card Clicks — Line 1486

When user clicks a preset card:
1. Get `data-preset` attribute
2. Create preset div via `createPresetDiv()`
3. If a node is selected → insert as child of selected node
4. If nothing selected → insert as child of root
5. Select the new node

### 13.2 Canvas Click — Line 1504

**Floating toolbar buttons:** Caught by `.struct-tool-btn` delegation
- Alignment buttons → `setProp("horizontalAlign", value)`
- add-child → `addChildDiv()`
- split-2 → `splitNode(id, 2)`
- clone → `duplicateNode()`
- delete → `removeNode()`
- move-up / move-down → `moveNode(id, -1/+1)`

**Div selection:** Click a `.struct-div` → set `State.selectedId` → re-render
**Deselection:** Click empty canvas area → `State.selectedId = null`

### 13.3 Tree Click — Line 1544

Click a `.tree-node` → set `State.selectedId` from `data-id` → re-render

### 13.4 Device Switcher — Line 1566

Click a `.device-btn` → set `State.device` → re-render
Frame CSS changes width based on `data-device` attribute.

### 13.5 Keyboard Shortcuts — Line 1592

| Keys | Action |
|---|---|
| `Ctrl/Cmd + Z` | Undo |
| `Ctrl/Cmd + Y` or `Ctrl/Cmd + Shift + Z` | Redo |
| `Delete` / `Backspace` (when body focused) | Delete selected node |

### 13.6 Left Panel Tabs — Line 1552

Toggles between "Palette" (preset cards) and "Tree" (DOM tree view).

---

## 14. CSS Architecture

### 14.1 Design System Variables (`:root`)

```css
--bg-base: #0a0d12        /* Darkest background */
--bg-surface: #111620     /* Panel backgrounds */
--bg-subtle: #18202d      /* Slightly lighter */
--bg-card: #1d2737        /* Card backgrounds */
--bg-hover: #263347       /* Hover states */
--line-base: #263447      /* Border color */
--line-bright: #394b65    /* Bright borders */
--fg-base: #f0f6fc        /* Primary text */
--fg-muted: #8b9bb4       /* Secondary text */
--fg-dim: #5a6b85         /* Disabled text */
--accent: #38bdf8         /* Primary blue */
--accent-soft: rgba(56,189,248,0.14)
--indigo: #6366f1         /* Purple accent */
--emerald: #10b981        /* Green accent */
--amber: #f59e0b          /* Orange accent */
--danger: #f43f5e         /* Red/delete */
--font-ui: "Plus Jakarta Sans"
--font-mono: "JetBrains Mono"
```

### 14.2 Layout Grid

```
.app-layout: CSS Grid
  ├── .topbar (row 1, full width)
  ├── .panel-left (row 2, col 1, 260px)
  ├── .stage (row 2, col 2, 1fr)
  ├── .panel-right (row 2, col 3, 320px)
  └── .statusbar (row 3, full width)
```

### 14.3 Key CSS Classes

| Class | Purpose |
|---|---|
| `.struct-div` | Canvas wireframe div (the visual blocks) |
| `.struct-div.is-selected` | Highlighted selected state |
| `.struct-div__tag` | `<div.className>` label badge |
| `.struct-div__toolbar` | Floating action toolbar |
| `.struct-tool-btn` | Toolbar button |
| `.tree-node` | DOM tree item |
| `.prop-card` | Inspector card container |
| `.prop-group` | Inspector field group |
| `.prop-label` | Inspector field label |
| `.prop-input` | Inspector text/number input |
| `.prop-seg` | Segmented button bar |
| `.prop-seg__btn` | Individual segment button |
| `.prop-row-2/3/4` | 2/3/4 column grid layout |
| `.prop-select` | Dropdown select element |
| `.quick-split-bar` | Action button row |
| `.btn-quick-action` | Quick action button |
| `.is-active-preset` | Highlighted preset button |
| `.has-custom-border` | Removes default wireframe border when custom border is set |

---

## 15. MCP Server

### 15.1 Architecture

```
Agent ──stdio──> index.js ──calls──> core.js ──manages──> In-memory State
```

- **Transport:** stdio (standard input/output)
- **SDK:** `@modelcontextprotocol/sdk` v1.12+
- **State:** In-memory, no file persistence (resets on restart)

### 15.2 All 21 Tools

Every mutating tool records history. Four of them (`reset_device`,
`import_json`, `set_breakpoints`, `reset_all`) previously did not, so `undo`
skipped straight past them — an agent could not undo a `reset_all`.

| # | Tool | Category | Mutates? | Has Undo? |
|---|---|---|---|---|
| 1 | `create_div` | Tree | ✅ | ✅ |
| 2 | `delete_div` | Tree | ✅ | ✅ |
| 3 | `move_div` | Tree | ✅ | ✅ |
| 4 | `clone_div` | Tree | ✅ | ✅ |
| 5 | `wrap_div` | Tree | ✅ | ✅ |
| 6 | `split_div` | Tree | ✅ | ✅ |
| 7 | `reparent_div` | Tree | ✅ | ✅ |
| 8 | `list_tree` | Tree | ❌ | — |
| 9 | `get_node` | Tree | ❌ | — |
| 10 | `set_props` | Props | ✅ | ✅ |
| 11 | `reset_device` | Props | ✅ | ✅ |
| 12 | `export_html` | Export | ❌ | — |
| 13 | `export_css` | Export | ❌ | — |
| 14 | `export_full` | Export | ❌ | — |
| 15 | `export_json` | Export | ❌ | — |
| 16 | `import_json` | Import | ✅ | ✅ |
| 17 | `set_breakpoints` | Config | ✅ | ✅ |
| 18 | `reset_all` | Config | ✅ | ✅ |
| 19 | `undo` | History | ✅ | — |
| 20 | `redo` | History | ✅ | — |
| 21 | `build_tree` | Tree | ✅ | ✅ |

### 15.2.4 `build_tree` — the fast path

Building a page a node at a time costs one round trip per call; the shipped
examples need **31 to 64** of them each, which is the dominant cost of driving
this over MCP. `build_tree` takes one compact nested spec instead:

```json
{ "class": "app-shell",
  "desktop": { "display": "grid", "customColumns": "260px 1fr" },
  "mobile":  { "customColumns": "1fr" },
  "children": [
    { "class": "sidebar", "desktop": { "position": "sticky", "top": "0" }, "mobile": { "hidden": true } },
    { "class": "content", "children": [ { "class": "card" }, { "class": "card" } ] }
  ] }
```

The device objects take exactly the properties `set_props` accepts. The spec is
validated before anything is created and reports the offending path
(`spec[0].desktop must be an object`), and the whole build is one undo step.

### 15.2.1 `export_json` / `import_json` envelope

The tree alone is not the whole document: breakpoints live beside it and decide
the `@media` values. `export_json` previously emitted the tree only, so a
document exported at 768/480 reimported at 992/576 with different media
queries. It now returns:

```json
{ "version": 2, "breakpoints": { "tablet": 992, "mobile": 576 }, "root": { ... } }
```

`import_json` accepts both this envelope and a bare legacy root node.

### 15.2.2 Import validation

`import_json` used to accept any shape and throw a `TypeError` out of a walk
deep inside the generator. It now validates and returns the offending path,
leaving state untouched:

```
Error: invalid tree — root.children[0] is missing responsive{}
```

### 15.2.3 History covers breakpoints

`History.snapshot()` captured only the tree, which would have stranded
`set_breakpoints`. It now snapshots `{ root, breakpoints }`. `reset_all` also
restores the default breakpoints, so a fresh start cannot inherit the previous
document's `@media` values.

### 15.3 MCP `core.js` vs Browser `builder.js`

Both are thin shells around the same `generator.js`.

| Feature | core.js (MCP) | builder.js (Browser) |
|---|---|---|
| DOM access | ❌ None | ✅ Full |
| Rendering | ❌ No UI | ✅ Canvas, Tree, Inspector |
| History | ✅ 50-deep stack | ✅ 50-deep stack |
| HTML/CSS emission | → `generator.js` | → `generator.js` |
| Export | ✅ HTML + CSS + JSON | ✅ HTML + CSS + Download |
| State | Module-level `state` | IIFE-scoped `State` |

Only the state wrappers differ:

```javascript
// mcp-server/core.js                    // builder.js
buildCss(state.root, state.breakpoints)  buildCss(State.root, State.breakpoints)
```

---

## 16. Edge Cases & Guards

### 16.1 BUG Fixes Applied

| Bug | Problem | Fix |
|---|---|---|
| **BUG 1** | `splitNode` on node with existing children duplicated them | Guard: only add children if `node.children.length === 0` |
| **BUG 2** | `getEffectiveProps` used truthiness check, so `0` and `""` were ignored | Uses `!== undefined` instead of truthiness |
| **BUG 4** | Grid children got `width:100%` forced, breaking grid stretch | Guard: skip width:100% when `parentDisplay === "grid"` |
| **BUG 5** | `flexGrow` applied to non-flex children | Guard: only apply when `parentDisplay === "flex"` |
| **BUG 6** | Multiple nodes with same class produced duplicate CSS selectors | Auto-suffix: `.col`, `.col-2`, `.col-3` |
| **BUG 7** | Block nodes had no `display` in CSS export | Always emit `display: block` |
| **BUG 8** | `justify-self` was emitted for `horizontalAlign` regardless of parent. Chrome implements CSS Box Alignment for block layout, so it forced every centered container to shrink-to-fit and burst the viewport — an empty div collapsed to 34px, a wrapper expanded to its 662px min-content inside 552px. `overflow-x: hidden` in the reset hid the damage: content was clipped, not scrollable | Gated on a grid parent. `margin-inline: auto` already does the centering |
| **BUG 9** | Flex-row children could not shrink below their content width, so a `nowrap` row burst its container | Emit `min-width: 0` on children of a flex row |
| **BUG 10** | Grid items have the same automatic minimum size, so one wide item widened its track until the whole grid overflowed | `min-width: 0` also emitted for children of a grid |
| **BUG 11** | `repeat(auto-fit, minmax(240px, 1fr))` cannot shrink below 240px | Emit `minmax(min(240px, 100%), 1fr)` |
| **BUG 12** | The breakpoint blocks were a hand-maintained `if` chain parallel to desktop; 16 of 43 properties were silently unreachable at tablet/mobile | Table-driven emission — see 12.2 |
| **BUG 13** | `hidden` was appended after the diff and read only raw overrides: no desktop support, and it could never be switched back on | `hidden` is the display value inside the declaration list |
| **BUG 14** | The canvas preview applied `align-self` and `width: fit-content` that the export never emitted | Preview mirrors the exported declarations exactly |
| **BUG 16** | `customClass` was interpolated raw into both `class="…"` and a CSS selector, so a name containing a quote or a space produced broken markup | `safeClass()` in `generator.js`, called by **both** emitters so the HTML and CSS can never name each other differently |
| **BUG 17** | `flex-grow`/`shrink`/`basis` were emitted when the node itself was `display:flex`. That is inverted — they style a flex **item** — so a flex container in a block parent got inert declarations while a real flex item got none | Gated on `ctx.flexParent` |
| **BUG 18** | The browser builder had no persistence: a refresh destroyed the whole tree | Debounced `localStorage` autosave, restored on boot, guarded by try/catch for private mode and quota |
| **BUG 15** | The three fixed preview widths (1280/768/375) all sat outside the 580–720px band where layouts broke | Free-width ruler drives the breakpoint from `State.breakpoints`, using the same rule the `@media` queries use |

### 16.2 Safety Guards

| Guard | Location | What it prevents |
|---|---|---|
| Root delete guard | `removeNode()` | Cannot delete the root node |
| Circular reparent guard | `reparentNode()` (MCP) | Cannot move a node into its own descendant |
| Boundary move guard | `moveNode()` | Cannot move before index 0 or past last sibling |
| Empty parent guard | `findParent()` | Returns null for root (root has no parent) |
| Device init guard | `setProp()` | Creates `responsive[device]` object if it doesn't exist |
| Debounce guard | Text inputs | Prevents re-render on every keystroke |

### 16.3 Migration Scenarios

| Scenario | What happens |
|---|---|
| Old node has `padding: 16` (single number) | `getEffectiveProps` expands to `paddingTop=16, paddingRight=16, paddingBottom=16, paddingLeft=16` |
| Node has no `responsive.tablet` | `getEffectiveProps` safely skips with `if (!overrides) return` |
| Node has `margin: "0 auto"` (old string format) | Kept as-is, new 4-side margin takes precedence |

---

## 17. Property Reference Table

Every CSS property the system can generate:

| Property | Inspector Control | Canvas Rendered? | CSS Exported? |
|---|---|---|---|
| `display` | Segmented: GRID/FLEX/BLOCK | ✅ | ✅ |
| `grid-template-columns` | Columns input (1-12) | ✅ | ✅ |
| `customColumns` | Text input (overrides cols) | ✅ | ✅ |
| `gridAutoMode` | Segmented: Fixed / Auto-Fit / Auto-Fill | ✅ | ✅ |
| `gridMinColWidth` | Text input (`200px`) | ✅ | ✅ |
| `gap` | Number input | ✅ | ✅ |
| `row-gap` | Number input | ✅ | ✅ |
| `justify-items` | Dropdown | ✅ | ✅ |
| `align-content` | Dropdown | ✅ | ✅ |
| `flex-direction` | Segmented: Row/Column | ✅ | ✅ |
| `flex-wrap` | Segmented: No Wrap/Wrap | ✅ | ✅ |
| `justify-content` | Dropdown | ✅ | ✅ |
| `align-items` | Dropdown | ✅ | ✅ |
| `width` | Text + presets | ✅ | ✅ |
| `height` | Text input | ✅ | ✅ |
| `min-height` | Number input | ✅ | ✅ |
| `max-width` | Text input | ✅ | ✅ |
| `max-height` | Text input | ✅ | ✅ |
| `aspect-ratio` | Presets: 1:1/4:3/16:9/21:9 | ✅ | ✅ |
| `padding` (4-side) | 4× number inputs + link toggle | ✅ | ✅ |
| `margin` (4-side) | 4× text inputs + link toggle | ✅ | ✅ |
| `margin-left/right: auto` | Alignment segmented bar | ✅ | ✅ |
| `justify-self` | Alignment segmented bar | ✅ | ✅ |
| `position` | Segmented: Static/Rel/Abs/Fixed/Sticky | ✅ | ✅ |
| `top/right/bottom/left` | 4× text inputs | ✅ | ✅ |
| `z-index` | Text input | ✅ | ✅ |
| `overflow` | Segmented: Visible/Hidden/Scroll/Auto | ✅ | ✅ |
| `overflow-x` | Dropdown select | ✅ | ✅ |
| `overflow-y` | Dropdown select | ✅ | ✅ |
| `background-color` | Color picker + hex input | ✅ | ✅ |
| `border` | Width + Style + Color | ✅ | ✅ |
| `border-radius` | Presets + custom input | ✅ | ✅ |
| `box-shadow` | Presets: None/sm/md/lg | ✅ | ✅ |
| `opacity` | Range slider 0-1 | ✅ | ✅ |
| `transform` | Presets: None/Lift/Shrink/Off-canvas + free text | ✅ | ✅ |
| `transition` | Presets: None/Fast/Base/Slow + free text | ✅ | ✅ |
| `backdrop-filter` | Presets: None/sm/md/Glass + free text. Emits the `-webkit-` prefix too (Safari needed it until 18) | ✅ | ✅ |
| `align-self` | Segmented: Auto/Start/Center/End/Stretch/Baseline | ✅ | ✅ |
| `grid-column: span` | Number input 1-12 | ✅ | ✅ |
| `flex-grow` | Number input | ✅ | ✅ |
| `flex-shrink` | Number input | ✅ | ✅ |
| `flex-basis` | Text input | ✅ | ✅ |
| `order` | Number input | ✅ | ✅ |
| `text-align` | Segmented: Left/Center/Right/Justify | ✅ | ✅ |
| `direction` | Segmented: Inherit/LTR/RTL | ✅ | ✅ |
| `display: none` (hidden) | Checkbox toggle on **every** device | ✅ | ✅ |

Every one of the 47 emittable declarations is overridable at **every** device.
`hidden` on desktop plus `hidden: false` at a breakpoint is the mobile-only
panel pattern.

---

## Initialization Flow

```
1. IIFE starts
2. Define utilities, defaults, State, History
3. Define all tree functions
4. Cache DOM element references
5. Define renderCanvasNode, renderCanvas, renderTree, renderProps, renderChrome
6. Define generateCleanHtml, generateResponsiveCss, generateFullHtmlDocument
7. Bind all event listeners (clicks, keyboard, export modal)
8. initDemo():
   - Create a "container" preset (flex column, 1200px max)
   - Create a "grid3" preset (3-column grid)
   - Nest grid3 inside container
   - Push both into State.root.children
   - Select the grid3 node
9. Call render() — first paint of canvas, tree, inspector, status bar
```

---

*Generated for Pure Responsive DIV Generator v5.0 — 1739 lines of JS, 1060 lines of CSS, 19 MCP tools.*

---

## 18. Verification

Layout correctness is measured, not eyeballed. The harness loads every example
in an iframe at each width from **320px to 1440px in 20px steps** (57 widths ×
10 examples = **570 checks**) and compares every div's `getBoundingClientRect()`
against the viewport edge.

This matters because the exported reset sets `overflow-x: hidden` on `body`, so
an overflowing element produces **no scrollbar** — `document.scrollWidth` stays
clean while content is silently clipped. Only per-element geometry finds it.

```
TOTAL FAILING WIDTHS: 0
PASS 01-simple-centered-card          clean 57/57
PASS 02-responsive-navbar             clean 57/57
...
PASS 10-editorial-magazine-grid       clean 57/57
```

### Content stress

The examples ship **empty**, so the width sweep only proves that an empty
skeleton holds. Since the project's contract is *"you build the structure, the
developer fills it"*, `test/content-stress.html` injects an unbreakable long
word, a bare URL, an oversized image, a wall of text and a wide table into every
leaf div across five layouts at five widths, and measures again.

Three guarantees in the exported reset make that pass:

```css
html, body { overflow-wrap: break-word; }        /* long words and URLs wrap  */
div { max-width: 100%; }                         /* no div exceeds its parent */
img, video, canvas, iframe, svg, table { max-width: 100%; }
```

`div { max-width: 100% }` is an *element* selector, so every class rule below it
wins — an authored `max-width` still applies where one is set on purpose. It
exists because `min-width: 0` is not enough on its own: a flex item in a
`flex-direction: column` parent with `align-items: flex-start` is sized to
max-content rather than shrunk, so a wide table inside it burst the layout by
1100px. A cap fixes that; a minimum cannot.

A property-coverage audit sets each property on each device and checks whether
it reaches the CSS: **47/47 on desktop, tablet and mobile**.

These checks are committed, not ad-hoc. `node test/run.js` runs three suites —
`regression` (one test per bug that shipped once), `property-coverage`, and
`examples` — and exits non-zero on failure; `test/responsive.html` is the browser
sweep. The suite was validated by mutation: re-introducing the `justify-self`
gate removal, the grid `min-width: 0`, and one-way `hidden` each failed a named
assertion.

`mcp-server/test.js` covers the 21 tools (10 smoke tests), and the examples are
verified honest two ways: each committed `.json` reproduces its committed
`.html` byte-for-byte through `importTreeJson` + `generateFullHtmlDocument`, and
re-running `generate_examples.js` reproduces every file byte-for-byte apart from
the timestamp-based node ids.
